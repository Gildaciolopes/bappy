import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#FFF",
    paddingHorizontal: 16,
    borderRadius: 8,
    width: "100%",
    height: 48,
    borderWidth: 1,
    borderColor: "#C3C5CB",
  },
});
